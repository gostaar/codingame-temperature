const n = parseInt().readLine();
const inputs = readline();

For (i = 0; i < n; i++) {
 const t = inputs[i]; //chaque valeur
 
 If (t > 0) {
  //t *= -1 

 t.sort(function(a, b) {
   return a-b;
 }

} else {
 
}
 
 t.sort(function(a, b) {
   return a-b;
 }); //Tri variable t

 console.log(inputs[0]); 



}
